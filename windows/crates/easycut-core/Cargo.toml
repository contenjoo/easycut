[package]
name = "easycut-core"
description = "EasyCut editing model and operations (port of the macOS app's Model layer)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
